export const tmpl = `
    <div class="chat">
        <div class="chat__sidebar">
            {{{profileLink}}}
            {{{searchInput}}}
            <div class="chat-card__list">
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
                {{{card}}}
            </div>
        </div>

        <div class="chat__content">
            {{{chatHeader}}}
            {{{chatList}}}
            {{{chatInput}}}
        </div>
    </div>
`;
